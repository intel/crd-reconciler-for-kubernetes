apiVersion: v1
kind: Service
metadata:
  name: "{{.Spec.StreamDataSpec.StreamName}}"
  labels:
    run: "{{.Spec.StreamDataSpec.StreamName}}"
spec:
  selector:
    run: "{{.Spec.StreamDataSpec.StreamName}}"
  ports:
  - port: 80
    targetPort: 80
